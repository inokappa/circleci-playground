version: 2.1

orbs:
  aws-cloudwatch-dashboard-annotation: inokappa/aws-cloudwatch-dashboard-annotation@0.0.5

executors:
  default:
    docker:
      - image: docker:19.03.5-git

  aws-cloudwatch-dashboard-annotation-demo:
    docker:
      - image: cimg/python:3.9

commands:
  install-dependencies:
    steps:
      - run:
          name: Install dependencies
          command: |
            apk update && apk add curl curl-dev bash sudo jq

  install-awscli:
    steps:
      - run:
          name: Install awscli
          command: |
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip" && \
              unzip awscliv2.zip && \
              sudo ./aws/install

jobs:
  test:
    executor:
      name: default
    steps:
      - run:
          name: Test
          command: |
            echo 'Test'

  build:
    executor:
      name: default
    steps:
      - run:
          name: Build
          command: |
            echo 'Build'

  deploy:
    executor:
      name: default
    steps:
      - run:
          name: Deploy
          command: |
            echo 'Deploy'

  aws-cloudwatch-dashboard-annotation-demo1:
    environment:
      AWS_PAGER: ''
    executor: aws-cloudwatch-dashboard-annotation-demo
    steps:
      - checkout
      - run:
          name: Your Deploy Action
          command: |
            echo "Your Deploy Action"
            echo "export DEPLOY_FINISHED_DATETIME=$(date -u '+%Y-%m-%dT%H:%M:%S.000Z')" >> $BASH_ENV
      - aws-cloudwatch-dashboard-annotation/add:
          dashboard_title: "ECS-dashboard"
          widget_title: "CPUUtilization"
          display_diff: "yes"
          deploy_finished_datetime: ${DEPLOY_FINISHED_DATETIME}

  aws-cloudwatch-dashboard-annotation-demo2:
    environment:
      AWS_PAGER: ''
    executor: aws-cloudwatch-dashboard-annotation-demo
    steps:
      - checkout
      - run:
          name: Your Deploy Action
          command: |
            echo "Your Deploy Action"
            echo "export DEPLOY_FINISHED_DATETIME=$(date -u '+%Y-%m-%dT%H:%M:%S.000Z')" >> $BASH_ENV
      - aws-cloudwatch-dashboard-annotation/add:
          dashboard_title: "ECS-dashboard"
          widget_title: "MemoryUtilization"
          display_diff: "yes"
          deploy_finished_datetime: ${DEPLOY_FINISHED_DATETIME}

  aws-cloudwatch-dashboard-annotation-demo3:
    environment:
      AWS_PAGER: ''
    executor: aws-cloudwatch-dashboard-annotation-demo
    steps:
      - checkout
      - run:
          name: Your Deploy Action
          command: |
            echo "Your Deploy Action"
            echo "export DEPLOY_FINISHED_DATETIME=$(date -u '+%Y-%m-%dT%H:%M:%S.000Z')" >> $BASH_ENV
      - aws-cloudwatch-dashboard-annotation/add:
          dashboard_title: "ECS-dashboard"
          widget_title: "CPUReservation"
          display_diff: "yes"
          deploy_finished_datetime: ${DEPLOY_FINISHED_DATETIME}

  aws-cloudwatch-dashboard-annotation-demo4:
    environment:
      AWS_PAGER: ''
    executor: aws-cloudwatch-dashboard-annotation-demo
    steps:
      - checkout
      - run:
          name: Your Deploy Action
          command: |
            echo "Your Deploy Action"
            echo "export DEPLOY_FINISHED_DATETIME=$(date -u '+%Y-%m-%dT%H:%M:%S.000Z')" >> $BASH_ENV
      - aws-cloudwatch-dashboard-annotation/add:
          dashboard_title: "ECS-dashboard"
          widget_title: "MemoryReservation"
          display_diff: "yes"
          deploy_finished_datetime: ${DEPLOY_FINISHED_DATETIME}

  aws-cloudwatch-dashboard-annotation-demo5:
    environment:
      AWS_PAGER: ''
    executor: aws-cloudwatch-dashboard-annotation-demo
    steps:
      - checkout
      - run:
          name: Your Deploy Action
          command: |
            echo "Your Deploy Action"
            echo "export DASHBOARD_TITLE=ECS-dashboard" >> $BASH_ENV
            echo "export WIDGET_TITLE=Invocations" >> $BASH_ENV
            echo "export DEPLOY_FINISHED_DATETIME=$(date -u '+%Y-%m-%dT%H:%M:%S.000Z')" >> $BASH_ENV
      - aws-cloudwatch-dashboard-annotation/add:
          dashboard_title: ${DASHBOARD_TITLE}
          widget_title: ${WIDGET_TITLE}
          display_diff: "yes"
          deploy_finished_datetime: ${DEPLOY_FINISHED_DATETIME}

workflows:
  version: 2

  test-only:
    jobs:
      - test:
          filters:
            branches:
              ignore: 
                - master
                - development

  build-after-deploy:
    jobs:
      - build:
          filters:
            branches:
              only: 
                - master
                - development
      - deploy:
          requires:
            - build

  aws-cloudwatch-dashboard-annotation-demo:
    jobs:
      - aws-cloudwatch-dashboard-annotation-demo1:
          filters:
            branches:
              only:
                - aws-cloudwatch-dashboard-annotation-demo
      - aws-cloudwatch-dashboard-annotation-demo2:
          filters:
            branches:
              only:
                - aws-cloudwatch-dashboard-annotation-demo
      - aws-cloudwatch-dashboard-annotation-demo3:
          filters:
            branches:
              only:
                - aws-cloudwatch-dashboard-annotation-demo
      - aws-cloudwatch-dashboard-annotation-demo4:
          filters:
            branches:
              only:
                - aws-cloudwatch-dashboard-annotation-demo
      - aws-cloudwatch-dashboard-annotation-demo5:
          filters:
            branches:
              only:
                - aws-cloudwatch-dashboard-annotation-demo
