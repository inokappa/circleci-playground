version: 2
jobs:
  build:
    docker:
      - image: docker:18.06.0-ce-git
    steps:
      - run:
          name: Hello world
          command: |
            echo "Hello CircleCI!!" > hello.txt
 
workflows:
  version: 2
  pr-build:
    jobs:
      - build:
          filters:
            branches:
              ignore:
                - master
